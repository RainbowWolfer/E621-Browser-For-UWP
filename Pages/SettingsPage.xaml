<Page
    x:Class="E621Downloader.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:E621Downloader.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

	<Page.Resources>
		<Style x:Key="TestKey" TargetType="TextBlock">
			<Setter Property="FontSize" Value="48"/>
		</Style>
		<Style x:Key="SectionTitle" TargetType="TextBlock">
			<Setter Property="HorizontalAlignment" Value="Center"/>
			<Setter Property="TextDecorations" Value="Underline"/>
			<Setter Property="FontSize" Value="64"/>
			<Setter Property="Margin" Value="30,10,20,10"/>
			<Setter Property="HorizontalAlignment" Value="Left"/>
		</Style>
	</Page.Resources>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>

		<VisualStateManager.VisualStateGroups>
			<VisualStateGroup>
				<VisualState x:Name="WideLayout">
					<VisualState.StateTriggers>
						<AdaptiveTrigger MinWindowWidth="800"/>
					</VisualState.StateTriggers>
					<VisualState.Setters>
						<Setter Target="SecondCol.(Grid.Row)" Value="0"/>
						<Setter Target="SecondCol.(Grid.Column)" Value="1"/>
						<Setter Target="SecondColumn.Width" Value="*"/>
					</VisualState.Setters>
				</VisualState>
				<VisualState x:Name="NarrowLayout">
					<VisualState.StateTriggers>
						<AdaptiveTrigger MinWindowWidth="0"/>
					</VisualState.StateTriggers>
					<VisualState.Setters>
						<Setter Target="SecondCol.(Grid.Row)" Value="1"/>
						<Setter Target="SecondCol.(Grid.Column)" Value="0"/>
						<Setter Target="SecondColumn.Width" Value="0"/>
					</VisualState.Setters>
				</VisualState>
			</VisualStateGroup>
		</VisualStateManager.VisualStateGroups>

		<TextBlock Grid.ColumnSpan="2" Text="Settings Page" FontSize="48" HorizontalAlignment="Center" Margin="10"/>

		<ScrollViewer Grid.Row="1">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*"/>
					<ColumnDefinition x:Name="SecondColumn" Width="*"/>
				</Grid.ColumnDefinitions>
				<Grid.RowDefinitions>
					<RowDefinition Height="auto"/>
					<RowDefinition Height="auto"/>
				</Grid.RowDefinitions>

				<StackPanel Grid.Row="0" Grid.Column="0" Orientation="Vertical">
					<TextBlock Text="Listing: " Style="{StaticResource SectionTitle}"/>
					<StackPanel Orientation="Horizontal" Margin="50,0,0,0">
						<TextBlock Text="Black List" VerticalAlignment="Center" FontSize="20" Margin="10"/>
						<Button Content="Manage" VerticalAlignment="Center" Margin="10" FontSize="20" Tapped="BlackListButton_Tapped"/>
					</StackPanel>
					<StackPanel Orientation="Horizontal" Margin="50,0,0,0">
						<TextBlock Text="Follow List" VerticalAlignment="Center" FontSize="20" Margin="10"/>
						<Button Content="Manage" VerticalAlignment="Center" Margin="10" FontSize="20" Tapped="FollowListButton_Tapped"/>
					</StackPanel>
					<TextBlock Text="Theme: " Style="{StaticResource SectionTitle}"/>
					<StackPanel Margin="40,0,0,0">
						<RadioButton Margin="5" Content="Light"/>
						<RadioButton Margin="5" Content="Dark"/>
						<RadioButton Margin="5" Content="Follow System" IsChecked="True"/>
					</StackPanel>
					<TextBlock Text="Local: " Style="{StaticResource SectionTitle}"/>
					<StackPanel Orientation="Vertical" Margin="40,0,0,0">
						<Button x:Name="DownloadPathButton" Content="Select Download Path" FontSize="20" Tapped="DownloadPathButton_Tapped" Margin="20,10,0,10" />
						<Button x:Name="ClearDownloadPathButton" Content="Clear" FontSize="20" Tapped="ClearDownloadPathButton_Tapped" Margin="20,0,0,10" />
						<TextBlock Name="DownloadPathTextBlock" Text="No Path Selected" VerticalAlignment="Center" Margin="20,0,0,10" FontSize="20"/>
					</StackPanel>
				</StackPanel>

				<StackPanel x:Name="SecondCol" Grid.Row="0" Grid.Column="1">
					<TextBlock Text="General: " Style="{StaticResource SectionTitle}"/>
					<StackPanel Margin="40,0,0,0">
						<ToggleSwitch x:Name="NullImageToggle" OffContent="Show NULL Image" OnContent="Show NULL Image" Margin="0,5,0,5" Toggled="NullImageToggle_Toggled"/>
						<ToggleSwitch x:Name="BlackListToggle" OffContent="Show Blacklisted" OnContent="Show Blacklisted" Margin="0,5,0,5" Toggled="BlackListToggle_Toggled"/>
						<ToggleSwitch x:Name="SafeModeToggle" OffContent="Enable Safe Mode" OnContent="Enable Safe Mode" Margin="0,5,0,5" Toggled="SafeModeToggle_Toggled"/>
						<ToggleSwitch x:Name="CycleListToggle" OffContent="Enable Cycle List" OnContent="Enable Safe Mode" Margin="0,5,0,5" Toggled="CycleListToggle_Toggled"/>
					</StackPanel>
					<TextBlock Text="About: " Style="{StaticResource SectionTitle}"/>
					<StackPanel Margin="40,0,0,0">
						<TextBlock FontSize="18" Text="Made By : RainbowWolfer"/>
						<TextBlock FontSize="18" Text="Source From : e621.net"/>
						<TextBlock FontSize="18" Text="{x:Bind Version}"/>
					</StackPanel>
				</StackPanel>
			</Grid>
		</ScrollViewer>
	</Grid>
</Page>
