<Page
	x:Class="E621Downloader.Pages.PicturePage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:E621Downloader.Pages"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:posts="using:E621Downloader.Models.Posts"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d"
	Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
	<Page.Resources>
		<CollectionViewSource x:Name="ContactsCVS" Source="{x:Bind tags}" IsSourceGrouped="True"/>
		<Style x:Key="TagButton" TargetType="Button">
			<Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
			<Setter Property="Background" Value="Transparent"/>
			<Setter Property="BorderThickness" Value="0"/>
			<Setter Property="Height" Value="40"/>
			<Setter Property="Width" Value="40"/>
		</Style>
		<Style x:Key="ScoreButton" TargetType="Button">
			<Setter Property="Background" Value="Transparent"/>
			<Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
			<Setter Property="BorderThickness" Value="0"/>
			<Setter Property="Height" Value="30"/>
			<Setter Property="FontSize" Value="15"/>
			<Setter Property="HorizontalContentAlignment" Value="Center"/>
			<!--<Setter Property="Padding" Value="0"/>-->
			<Setter Property="HorizontalAlignment" Value="Stretch"/>
			<Setter Property="Margin" Value="3"/>
		</Style>
	</Page.Resources>
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="250"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>
		<StackPanel Grid.Column="1">
			<StackPanel Orientation="Horizontal">
				<Button x:Name="DebugButton" Content="Debug Post Info" FontSize="18" Margin="10" Tapped="DebugButton_Tapped"/>
				<TextBlock Margin="10" Text="{x:Bind Title}" FontSize="30" VerticalAlignment="Center" TextWrapping="WrapWholeWords" HorizontalTextAlignment="Center"/>
			</StackPanel>
			<RelativePanel Margin="5">
				<Button x:Name="DownloadButton" Content="Download" FontSize="18" Margin="5" Tapped="DownloadButton_Tapped"/>
				<Button x:Name="CopyButton" Content="Copy" FontSize="18" Margin="5" Tapped="CopyButton_Tapped" RelativePanel.RightOf="DownloadButton"/>
				<Button x:Name="BrowserButton" Content="Open In Default Browser" FontSize="18" Margin="5" Tapped="BrowserButton_Tapped" RelativePanel.RightOf="CopyButton"/>
				<Button x:Name="MoreInfoButton" RelativePanel.AlignRightWithPanel="True" FontSize="18" Background="Transparent" BorderThickness="0" Height="38" RelativePanel.AlignVerticalCenterWithPanel="True" Tapped="MoreInfoButton_Tapped">
					<Button.Content>
						<StackPanel Orientation="Horizontal">
							<TextBlock Text="More Info" Margin="5"/>
							<FontIcon Glyph="&#xE169;" Margin="5"/>
						</StackPanel>
					</Button.Content>
				</Button>
			</RelativePanel>
		</StackPanel>

		<ListView x:Name="TagsListView" ItemsSource="{Binding Source={StaticResource ContactsCVS}}" Grid.RowSpan="2" SelectionMode="None" IsItemClickEnabled="True" ItemClick="TagsListView_ItemClick">
			<ListView.ItemsPanel>
				<ItemsPanelTemplate>
					<ItemsStackPanel AreStickyGroupHeadersEnabled="True"/>
				</ItemsPanelTemplate>
			</ListView.ItemsPanel>
			<ListView.ItemTemplate>
				<DataTemplate x:DataType="x:String">
					<RelativePanel Width="225">
						<Button x:Name="InfoButton" FontFamily="Segoe MDL2 Assets" Content="&#xE946;" RelativePanel.AlignVerticalCenterWithPanel="True" Style="{StaticResource TagButton}" Tapped="InfoButton_Tapped" Tag="{x:Bind ToString()}"/>
						<TextBlock Text="{x:Bind ToString()}" RelativePanel.AlignVerticalCenterWithPanel="True" Margin="2,0,20,0" TextWrapping="Wrap" Width="100" RelativePanel.RightOf="InfoButton"/>
						<Button x:Name="BlackListButton" Style="{StaticResource TagButton}" Content="&#xF8AB;" Tag="{x:Bind ToString()}" RelativePanel.LeftOf="FollowListButton" Tapped="BlackListButton_Tapped" Loaded="BlackListButton_Loaded"/>
						<Button x:Name="FollowListButton" Style="{StaticResource TagButton}" Content="&#xF8AA;" Tag="{x:Bind ToString()}" RelativePanel.AlignRightWithPanel="True" Tapped="FollowListButton_Tapped" Loaded="FollowListButton_Loaded"/>
					</RelativePanel>
				</DataTemplate>
			</ListView.ItemTemplate>
			<ListView.GroupStyle>
				<GroupStyle>
					<GroupStyle.HeaderTemplate>
						<DataTemplate>
							<TextBlock Text="{Binding Key}" Margin="10,0" FontWeight="Bold" />
						</DataTemplate>
					</GroupStyle.HeaderTemplate>
				</GroupStyle>
			</ListView.GroupStyle>
		</ListView>
		<SplitView x:Name="MainSplitView" DisplayMode="Overlay" Grid.Column="1" Grid.Row="1" PanePlacement="Right" OpenPaneLength="500">
			<SplitView.Pane>
				<Grid>
					<Pivot>
						<PivotItem Header="Description">
							<ScrollViewer>
								<TextBlock Text="{x:Bind PostRef.description}" TextWrapping="Wrap" FontSize="18" Margin="20" />
							</ScrollViewer>
						</PivotItem>
						<PivotItem Header="Comments">
							<Grid>
								<StackPanel x:Name="LoadingSection" Visibility="Collapsed" Grid.Row="1" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
									<TextBlock Text="Loading Comments" FontSize="22" Margin="5" />
									<ProgressBar IsIndeterminate="True" Margin="5" />
								</StackPanel>

								<ListView x:Name="CommentsListView" ItemsSource="{x:Bind comments}" Grid.Row="1" SelectionMode="None" IsItemClickEnabled="False">
									<ListView.ItemContainerStyle>
										<Style TargetType="ListViewItem">
											<Setter Property="HorizontalContentAlignment" Value="Stretch" />
										</Style>
									</ListView.ItemContainerStyle>
									<ListView.ItemTemplate>
										<DataTemplate x:DataType="posts:E621Comment">
											<Grid>
												<Grid.ColumnDefinitions>
													<ColumnDefinition Width="140" />
													<ColumnDefinition Width="*" />
												</Grid.ColumnDefinitions>
												<Grid.RowDefinitions>
													<RowDefinition Height="auto" />
													<RowDefinition Height="*" />
													<RowDefinition Height="auto" />
													<RowDefinition Height="auto" />
													<RowDefinition Height="auto" />
												</Grid.RowDefinitions>
												<Image VerticalAlignment="Top" Grid.Column="0" Grid.Row="0" Height="120" Width="120" Source="/Assets/StoreLogo.png" />
												<TextBlock VerticalAlignment="Top" FontSize="14" Grid.Column="0" Grid.Row="1" Text="{x:Bind creator_name}" HorizontalAlignment="Center" />
												<Grid Grid.Column="0" Grid.Row="2" Margin="10,0,10,0">
													<Grid.ColumnDefinitions>
														<ColumnDefinition Width="*" />
														<ColumnDefinition Width="*" />
														<ColumnDefinition Width="*" />
													</Grid.ColumnDefinitions>
													<Button Content="&#xE96D;" Style="{StaticResource ScoreButton}" Grid.Column="0" />
													<TextBlock Text="{x:Bind score}" Grid.Column="1" FontSize="16" HorizontalAlignment="Stretch" TextAlignment="Center" VerticalAlignment="Center"/>
													<Button Content="&#xE96E;" Style="{StaticResource ScoreButton}" Grid.Column="2" />
												</Grid>
												<Grid Grid.Row="3">
													<Grid.ColumnDefinitions>
														<ColumnDefinition Width="*" />
														<ColumnDefinition Width="*" />
													</Grid.ColumnDefinitions>
													<Button HorizontalAlignment="Stretch" Content="Reply" Grid.Column="0" Margin="5" />
													<Button HorizontalAlignment="Stretch" Content="Report" Grid.Column="1" Margin="5" />
												</Grid>
												<Grid Grid.Row="4" Grid.ColumnSpan="2">
													<Rectangle HorizontalAlignment="Stretch" Fill="Gray" Height="2" Margin="0,5,0,10" />
												</Grid>
												<Grid Grid.Column="1" Grid.RowSpan="4">
													<TextBlock Margin="5" FontSize="22" Text="{x:Bind body}" TextWrapping="Wrap" />
												</Grid>
											</Grid>
										</DataTemplate>
									</ListView.ItemTemplate>
								</ListView>
							</Grid>
						</PivotItem>
					</Pivot>
				</Grid>
			</SplitView.Pane>
			<SplitView.Content>
				<Grid>
					<MediaPlayerElement x:Name="MyMediaPlayer" AutoPlay="False" AreTransportControlsEnabled="True" Margin="0,0,40,40"/>
					<ScrollViewer x:Name="MyScrollViewer" MinZoomFactor="0.3" DoubleTapped="MyScrollViewer_DoubleTapped" ZoomMode="Enabled" HorizontalScrollMode="Enabled" HorizontalScrollBarVisibility="Hidden" VerticalScrollMode="Enabled" VerticalScrollBarVisibility="Hidden">
						<Image PointerWheelChanged="MainImage_PointerWheelChanged" PointerPressed="MainImage_PointerPressed" PointerReleased="MainImage_PointerReleased" PointerMoved="MainImage_PointerMoved" PointerEntered="MainImage_PointerEntered" PointerExited="MainImage_PointerExited" Stretch="Uniform" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Name="MainImage" Margin="0,0,40,40" ImageOpened="MainImage_ImageOpened"/>
					</ScrollViewer>
					<ProgressRing Name="MyProgressRing" MaxHeight="300" MaxWidth="300" Margin="200" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" IsActive="True"/>
				</Grid>
			</SplitView.Content>
		</SplitView>

	</Grid>
</Page>
