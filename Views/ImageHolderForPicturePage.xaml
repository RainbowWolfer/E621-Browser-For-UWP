<UserControl
    x:Class="E621Downloader.Views.ImageHolderForPicturePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:E621Downloader.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="330"
    d:DesignWidth="400"
	d:RequestedTheme="Dark"
	Height="330"
	Width="400">


	<Grid CornerRadius="10">
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="auto"/>
		</Grid.RowDefinitions>
		<Rectangle Fill="#444444" Grid.RowSpan="2"/>
		<Image x:Name="PreviewImage" Stretch="Uniform"/>
		<Image x:Name="MyImage" Stretch="Uniform"/>
		<TextBlock x:Uid="DeletedPostHintText" x:Name="HintText" Text="Post was Deleted" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="36" Visibility="Collapsed" TextWrapping="Wrap" TextAlignment="Center"/>
		<ProgressRing x:Name="LoadingRing" Width="200" Height="200" Margin="50" IsActive="True"/>
		<local:TypeHintForImageHolder x:Name="TypeHint"/>
		<Grid Grid.Row="1" Margin="5,0,5,0">
			<local:BottomInformationForImageHolder x:Name="BottomInfo"/>
		</Grid>
	</Grid>
</UserControl>
