<UserControl
    x:Class="E621Downloader.Views.ImageHolderForSubscriptionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:E621Downloader.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    d:RequestedTheme="Dark">

    <Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="auto"/>
		</Grid.RowDefinitions>
		<!--Make sure that right tap works on empty image-->
		<Rectangle HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Fill="Gray" Opacity="0.01"/>
		<TextBlock x:Name="HintText" Text="Failed" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="24" Visibility="Collapsed"/>
		<Image x:Name="MyImage" Stretch="Uniform"/>
		<controls:ProgressRing x:Name="LoadingRing" Margin="50" MinHeight="100" MinWidth="100"/>
		<local:TypeHintForImageHolder x:Name="TypeHint"/>
		<Grid Grid.Row="1" Margin="5,0,5,0">
			<local:BottomInformationForImageHolder x:Name="BottomInfo"/>
		</Grid>
	</Grid>
</UserControl>
