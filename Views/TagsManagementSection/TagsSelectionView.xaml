<Page
    x:Class="E621Downloader.Views.TagsManagementSection.TagsSelectionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:E621Downloader.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="400"
    Width="300">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="auto"/>
		</Grid.RowDefinitions>
		<AutoSuggestBox Name="MySuggestBox" TextChanged="MySuggestBox_TextChanged" Margin="10"/>
		<ListView Grid.Row="1" ItemsSource="{x:Bind tags}" SelectionMode="None" IsItemClickEnabled="False">
			<ListView.ItemContainerStyle>
				<Style TargetType="ListViewItem">
					<Setter Property="HorizontalContentAlignment" Value="Stretch"/>
					<Setter Property="VerticalContentAlignment" Value="Stretch"/>
				</Style>
			</ListView.ItemContainerStyle>
			<ListView.ItemTemplate>
				<DataTemplate x:DataType="x:String">
					<Grid>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="auto"/>
							<ColumnDefinition Width="*"/>
							<ColumnDefinition Width="auto"/>
						</Grid.ColumnDefinitions>
						<Button x:Name="InfoButton" Tag="{x:Bind ToString()}" Height="30" Width="30" Background="Transparent" BorderThickness="0" FontFamily="Segoe MDL2 Assets" Content="&#xE946;" Tapped="InfoButton_Tapped"/>
						<TextBlock Grid.Column="1" Text="{x:Bind ToString()}" VerticalAlignment="Center" FontSize="18" Margin="5,0,0,3"/>
						<Button x:Name="DeleteButton" Tag="{x:Bind ToString()}" Grid.Column="2" FontFamily="Segoe MDL2 Assets" Content="&#xE106;" Background="Transparent" BorderThickness="0" Height="30" Width="30" Tapped="DeleteButton_Tapped"/>
					</Grid>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>
		<Grid Grid.Row="2">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*"/>
				<ColumnDefinition Width="*"/>
			</Grid.ColumnDefinitions>
			<Button x:Name="SearchButton" Content="Search" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0,10,10,0" Tapped="SearchButton_Tapped"/>
			<Button x:Name="DialogBackButton" Content="Back" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.Column="1" Margin="10,10,0,0" Tapped="DialogBackButton_Tapped"/>
		</Grid>
	</Grid>
</Page>
